to initialse gitbash from scratch:
touch .gitignore
git config --global user.name Yash
git config --global user.email youremail@gmail.com
git init
git add .
git commit -m "Initial Commit"
git status #just for refrence(optional)

if(ssh =-key is not established):
ssh-keygen -t rsa -b 4096 -C "your-email.com@example.com"
tail<path to id_rsa.pub> # add this key to your github account

git remte add origin <ssh- url.git>
git push origin master
